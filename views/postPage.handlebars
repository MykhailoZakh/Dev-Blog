{{#with post}}{{>post-card}} {{/with}}

<header class="comm-line"><p id="comm-head">Comments area ⬇</p><button id="comm-button">Add Comment</button> </header>

<form class="form" id="hidden">
    <header class="form-header" >Create New Comment</header>

    <p class="form-p">
        <label class="form-label" for="content">Comment</label>
        <input class="form-input" type="text" id="post-text" name="text" placeholder="Your Text">
    </p>
   
    <button class="form-btn" id="post-createBtn">Create</button>
</form>

{{#each comments as |content|}} {{>comment-card}} {{/each}}

<script src="/js/comment.js"></script>
{{!-- <p>text</p> --}}